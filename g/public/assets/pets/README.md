Place custom pet assets here.

Folders:
- `eggs/` for egg artwork
- `puppies/` for puppy and evolution artwork

The central data file is:
- `g/src/game/content/pet-eggs.json`

Update the image paths in that JSON if you add new filenames.
