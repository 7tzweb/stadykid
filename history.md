# History

## 2026-04-03
- תוקן ארון הבגדים ב-`PetHomeScreen`: פריטי לבוש ואביזרים שנקנו מוצגים עכשיו עם התמונה האמיתית שלהם ולא רק כאימוג'י כללי, והפריטים החדשים ביותר מופיעים ראשונים כדי שיהיה קל למצוא מיד אחרי קנייה. גם הסימון על הגור עצמו משתמש עכשיו בתצוגת הפריט האמיתית כשיש תמונה.
- תוקנה תנועת מפת השלבים ב-`WorldMapScreen` לטאבלט: במקום לחסום `touch`, ה-viewport של המפה משתמש עכשיו בגלילה/החלקה טבעית עם אצבע (`pan-x/pan-y`), בזמן שבמחשב נשמרת גם גרירת עכבר.
- בוצע audit גם לשאר סוגי השאלות: `DragAndDropGame` מערבב עכשיו גם את הפריטים וגם את היעדים בכל שאלה, ו-`MatchPairsGame` מערבב את שני הצדדים במקום רק צד אחד, כדי שהפתרון לא יופיע שוב ושוב באותו סדר קבוע.
- תוקן גנרטור השלבים `generate-age-levels.mjs` כך שלא ייווצרו יותר תשובות כפולות באותה שאלה, במיוחד במסלולי `forest-of-numbers`. נוספה גם ולידציה ב-`level-schema` שתופסת כפילויות כאלה בזמן טעינת שלב.
- תוקן `MultipleChoiceGame` כך שסדר התשובות בשאלות בחירה כבר לא נשאר לפי סדר ה-`JSON`: בכל שאלה מיקום התשובה הנכונה נבחר מחדש בצורה רנדומלית, וגם המסיחים עצמם עוברים ערבוב.
- נוספה תצורת בדיקות קבועה לטאבלט: סקריפט `npm run dev:tablet` על פורט `5174`, סקריפט הפעלה ייעודי, ו-`LaunchAgent` במק (`com.gamekids.tablet-dev`) שמעלה מחדש את שרת Vite אוטומטית לצורך בדיקות מהטאבלט.
- לפריט `moon-pillow` נוספה תמונת `PNG` שקופה חדשה (`/assets/shop/props/moon-pillow.png`) במקום fallback של אייקון מיטה, כדי שהחפץ יוצג בחדר ובמגש כפרופ אמיתי בלי רקע לבן.
- נוספה תמיכה בהחזרת חפצים מהחדר ב-`PetHomeScreen`: לכל חפץ שמונח בחדר מופיע במגש החפצים כפתור `להחזיר`, והפעולה מסירה אותו מ-`placedProps` בלי למחוק אותו מהמלאי.
- נוספו למסך `PetHomeScreen` שני כפתורי ניווט חדשים ליד מוני הכוכבים והמטבעות בחלק העליון: `חנות` למעבר ישיר ל-`/shop` ו-`מפה` לחזרה מהירה ל-`/worlds`.
- הוגדלה מעט תצוגת החפצים שכבר נמצאים בחדר ב-`PetHomeScreen`, כולל צל ו-scale מעודכנים, כדי שיהיו קריאים ובולטים יותר על הרקע.
- נוספה אפשרות להזיז ידנית חפצים שכבר הונחו בחדר ב-`PetHomeScreen`: חפצים נשארים סטטיים ולא זזים לבד, אבל מעכשיו אפשר לגרור אותם מחדש לכל מקום בחדר גם אחרי ההצבה, בטאבלט ובמחשב.
- חוזקה מחדש גרירת האוכל לבית הגורים ב-`PetHomeScreen`: שחרור אוכל על גור מזוהה עכשיו לפי כל אזור הגור עם מרווח ביטחון נוסף למגע, ולא רק לפי פגיעה מדויקת באלמנט פנימי. בנוסף `pointermove` של גרירה מונע גלילה בזמן drag, כדי שהאכלה בגרירה תעבוד טוב יותר גם בטאבלט וגם במחשב.
- תוקנה פריסת `GameScreen` לטאבלט ולמובייל כך שאזור השאלה והמשחק נשאר ראשון בחלק העליון, ו`לוח ההצלחות` כבר לא קופץ מעליו במסכים מתחת ל-`xl`.
- בוצע audit לטאבלט ולמסכי מגע לאורך האתר, עם דגש על פערים מול מחשב/עכבר: נוספה שכבת `touch` כללית ללחצנים דרך `g/src/index.css`, שופרה תמיכת המגע ב-`DragAndDropGame`, ונמנעו אינטראקציות שנשענות רק על `hover` או `HTML5 drag` במקומות הקריטיים.
- תוקן `PetHomeScreen` כך שפריטי האוכל והחפצים יעבדו טוב יותר בטאבלט: גרירה מתחילה רק אחרי תזוזה אמיתית, יש ניקוי מסודר ב-`pointercancel`, ונוסף fallback לטאפ בודד כך שאוכל יכול להישלח לגור הרעב/הנבחר גם בלי גרירה, וחפצים יכולים להיכנס לחדר גם בלחיצה.
- תוקן `g/src/game/runtime/DragAndDropGame.tsx` כך שבמכשירי מגע המשחק עובד במודל ברור של `לוחצים על פריט ואז על יעד`, בזמן שבמחשב עדיין נשמרת גם אפשרות הגרירה.
- עודכנה לוגיקת הצרכים של הגורים ב-`g/src/game/content/creatures.ts` כך שבכל רגע נבחר צורך בסיסי פעיל אחד בלבד (`feed/pet/play/rest`) במקום מירוץ קבוע של כל הצרכים במקביל. הבחירה עכשיו pseudo-random, מאוזנת לפי היסטוריית הטיפול, מפחיתה חזרות מיידיות ומוסיפה השהיות שונות בין גורים כדי שלא יתקבל שוב ושוב אותו סדר בקשות.
- עודכן `PetHomeScreen` כדי ליישר את מסך הבית עם הלוגיקה החדשה בלי לשבור את בועות האוכל, הלב, המשחק, המנוחה והבקשות המיוחדות, ובאותה הזדמנות סודרו גם בעיות TypeScript/React Hooks שנחשפו בזמן הבדיקה.
- לאחר השינויים בוצעו `npm run lint` ו-`npm run build` בתוך `g`; ה-build עבר בהצלחה, וה-lint עבר בלי שגיאות עם אזהרת `React Hooks` קיימת אחת ב-`PetHomeScreen`.

## 2026-03-15
- בוצע redesign למסך `PetHomeScreen` כדי להפוך אותו למסך בית אחד קבוע ופשוט לילדים: בהשראת `codedisian/worlddisian/bg1.html`, עם סרגל ביצים וגורים משמאל, עולם משחק גדול במרכז, אזור פעולות מימין, ותפריט עליון ליציאה לעולם השאלות, לחנות, להתקדמות ולהגדרות.
- הוסרו החדרים הפנימיים והצלילים הזמניים מהבית הראשי, ובמקומם נוספו תנועה איטית יותר לגורים, עצירות רנדומליות, תמונות יצור בלי מסגרות כרטיס, וגרירת אוכל/שתייה ישירות אל הגור כדי להאכיל אותו.
- בוצע מעבר נוסף למסך `full-screen` אמיתי: העולם הראשי תופס עכשיו את כל הגובה והרוחב, הפאנלים הקבועים הוחלפו בכפתורי אייקון צפים ומגשים נפתחים, ורוב הטקסטים במסך הוסרו כדי לשמור על UI פשוט וברור לילדים.
- בועות הצורך של הגורים הוחלפו לגרסת אייקון בלבד, ובליטוף מופיעה אנימציית לב אדום יחד עם בועת `תודה!`.
- חולץ הרקע המקורי מתוך `codedisian/worlddisian/bg1.html` לקובץ `g/public/assets/world/playroom-bg1.png`, וחובר כעת כרקע הראשי של העולם.
- תנועת הגורים שונתה מתנועה על מסלולים לתנועה חופשית בכל שטח העולם, ונוספה אפשרות לגרור את הגור ידנית למקום חדש על המסך.
- חוזקה הקריאות של הכפתורים הצפים והנתונים העליונים באמצעות רקעי glass ברורים יותר, ונוסף בחלק העליון כפתור `משחקים` בולט שמוביל לעולם המשחקים עם רמז לתגמולי כוכבים וגורים.
- עודכן `WorldMapScreen`: כפתור החזרה לבית הוחלף לכפתור ראשי ובולט של `עולם הגורים`, והוסרו אזורי ההסבר המיותרים בחלק העליון ובצד כדי לפשט את מסך המפה.
- נבנה מסך בית ראשי חדש `PetHomeScreen` כמרכז המשחק, ובהמשך הוא פושט למסך עולם אחד קבוע ופשוט יותר שבו הגורים הם הפוקוס המרכזי.
- נוספה מערכת יצורים מלאה מבוססת `JSON` דרך `g/src/game/content/creatures.json`, כולל מחיר בכוכבים, תמונת ביצה, תמונת גור, תמונת שלב גדילה, זמני בקיעה, צרכים, קולות ודרישות גדילה.
- נבנתה שכבת לוגיקה חדשה ב-`g/src/game/content/creatures.ts` לחישוב בקיעה, צרכים, מצב רוח, שלב גדילה נוכחי, שלב הבא וטיימרים.
- הורחב `GameState` עם `stars` ו-`ownedCreatures`, ונוספו פעולות store חדשות: קניית יצור בכוכבים, הצבה/הסרה מהבית, טיפול (`feed/pet/play/rest`) והלבשת פריטים קיימים.
- שונתה זרימת האפליקציה כך שבחירת ילד מובילה עכשיו למסלול `/home`, ומשם יוצאים לעולם השאלות (`/worlds`), לחנות, להתקדמות ולהגדרות.
- מסך הבית החדש כולל סרגל צד של ביצים וגורים, תנועה אוטומטית של יצורים בעולם, בועות צורך, אינטראקציות טיפול, תגמול כוכבים וציוד לביש.
- עודכנה החנות כך שביצים של יצורים נקנות עם כוכבים במקום מטבעות, בעוד בגדים ואביזרים ממשיכים להיקנות במטבעות.
- עודכנו `MissionCompleteScreen`, `WorldMapScreen` ו-`ProgressScreen` כך שיציגו כוכבים ויעבדו עם מודל היצורים החדש והבית הראשי.
- נשמרה תמיכה בנתוני עבר דרך migration מ-`petEggs` הישנים ל-`ownedCreatures` החדשים.
- לאחר כל השינויים בוצעו `npm run lint` ו-`npm run build` בתוך `g` בהצלחה.

## 2026-03-14
- עודכן מסך החנות כך שקטגוריית `ביצים` מופיעה ראשונה, וכרטיסי הביצים מציגים תמונה גדולה על כל הכרטיס בלי טקסט תיאורי קטן.
- נוספה מערכת ביצים וגורים מבוססת `JSON` דרך הקובץ `g/src/game/content/pet-eggs.json`, כולל מחיר, זמן בקיעה, נתיב תמונת ביצה, שלבי גור והתפתחות עתידית.
- נוספה תשתית עזר בקובץ `g/src/game/content/petEggs.ts` לחישוב סטטוס בקיעה, טיימר, שלב התפתחות נוכחי והשלב הבא.
- הורחב ה-`GameState` עם `petEggs`, ונוספה פעולת `buyPetEgg()` ששומרת זמן רכישה ומורידה מטבעות רק בקניית ביצה חדשה.
- מסך `ShopScreen` חובר לטיימר חי של בקיעה, ומסך `ProgressScreen` מציג את הביצים שנרכשו, את הגורים שבקעו ואת ההתקדמות לשלב ההתפתחות הבא.
- נוספו תיקיות סטטיות ו-placeholder assets בתוך `g/public/assets/pets/eggs` ו-`g/public/assets/pets/puppies`, יחד עם `README.md` שמסביר היכן להחליף תמונות.
- הופעלה תמיכה ב-`resolveJsonModule` ב-`TypeScript` כדי לייבא את קטלוג הביצים ישירות מה-JSON.
- לאחר השינויים בוצעו `npm run lint` ו-`npm run build` בתוך `g` בהצלחה.

## 2026-03-12
- סודר מחדש הקובץ `begin.md` למסמך דרישות אחד ברור ומאורגן.
- נשמר סעיף הכללים המחייבים והורחב עם כללים נוספים חשובים לארכיטקטורה, JSON, Firebase, responsive ו-reuse.
- נוספו מסכים חסרים ותוקנה ההפרדה בין מעטפת React לבין מנוע המשחקים ב-Phaser.
- הוקם פרויקט מלא בתוך `/Users/tsoharzigdon/gamekids/g` עם `React`, `TypeScript`, `Vite`, `TailwindCSS`, `React Router`, `Firebase`, `Phaser` ו-`Capacitor`.
- נבנו כל המסכים הראשיים: `Splash`, `Auth`, `Child Profiles`, `Child Editor`, `Avatar Builder`, `World Map`, `Mission Selection`, `Game`, `Mission Complete`, `Progress`, `Shop`, `Parent Gate`, `Parent Dashboard`, `Settings`, `System Status` ו-`Not Found`.
- נבנתה תשתית state עם `GameContext`, שמירה ל-`localStorage`, הכנה לסנכרון ל-`Firestore`, וחיבור בסיסי ל-`Google Sign-In`.
- נבנה מנוע שלבים מבוסס `JSON` עם `schema`, `parser`, `registry`, ו-renderers לארבעת סוגי השלבים: `multiple_choice`, `drag_and_drop`, `match_pairs`, `memory_cards`.
- נוספו 5 קבצי `JSON` לדוגמה בתוך `g/public/levels` וחוברו למסך המשחק דרך מערכת טעינת שלבים.
- בוצע code-splitting למסכים, פיצול ייעודי ל-`Phaser`, ונבדק שהפרויקט עובר `npm run build` ו-`npm run lint` בהצלחה.
- הורץ שרת פיתוח מקומי מתוך `g` עם `npm run dev -- --host 0.0.0.0 --port 4173` לצורך בדיקה והרצה של המשחק בדפדפן.
- בוצע מעבר רוחבי על ה-UI כדי לאחד את הטקסטים לעברית ולבטל ניסוחים מעורבים באנגלית במסכים הגלויים לילדים.
- נוסף רכיב `StepGuide` reusable שמציג הנחיות צעד-אחר-צעד, וחובר למסכי הפתיחה, ההתחברות, בחירת הפרופיל, בניית הדמות, מפת העולמות, בחירת המשימה ומסך המשחק.
- שונתה זרימת הילדים כך שכפתור הפתיחה הראשי מוביל ישירות לבחירת פרופיל, והכפתורים במסכי הבחירה נוסחו מחדש כדי שיהיה ברור מאוד מה ללחוץ כדי להתחיל משחק.
- תורגמו שמות עולמות, כותרות של רכיבי משחק, מסכי סיום/התקדמות/חנות/הגדרות/אזור הורים, ונוספו מיפויי תוויות לנושאים ולקטגוריות חנות.
- עודכן גם תוכן השלב `logic-cave-001.json` כך שהמונחים עצמם מוצגים בעברית.
- לאחר השינויים בוצעו שוב `npm run lint` ו-`npm run build` בהצלחה.
- תוקנה שגיאת טעינה במסלול `game/forest-numbers-002`: נוספו כל קובצי השלבים החסרים עם סיומת `002` לתיקיית `g/public/levels`.
- הוקשח `levelService` כך שיזהה מצב שבו שרת הפיתוח מחזיר `HTML` במקום `JSON`, ויחזיר שגיאה ברורה בעברית במקום שגיאת `Unexpected token '<'`.
- לאחר תיקון השלבים ושירות הטעינה בוצעו שוב `npm run lint` ו-`npm run build` בהצלחה.
- שונתה פריסת `GameScreen` כדי לדחוס את אזור המשחק למסך ראשון בדסקטופ: אזור ה-Phaser והמשימה עברו לפריסה דו-עמודית במקום מבנה אנכי ארוך.
- כרטיס ההנחיות במסך המשחק הוחלף בגרסה קומפקטית יותר, ורכיבי המשחק (`multiple choice`, `drag and drop`, `match pairs`, `memory cards`) הוקטנו כדי לצמצם גובה אנכי.
- לאחר שינוי הפריסה בוצעו שוב `npm run lint` ו-`npm run build` בהצלחה.
- ב-`PetHomeScreen` חודד סימון הלב של הליטוף: הכפתור הוגדל, קיבל הדגשה אדומה ואנימציית פעימה, ונוספה תווית `👆 לחצו` כדי שיהיה ברור שזה כפתור מגע.
- מנגנון הליטוף עודכן מחדש: הוסרו בועות הטקסט, ליטוף נעשה בלחיצה ישירה על הגור, הלב מתמלא בהדרגה מעליו, ובמילוי מלא מתקבל כוכב. בנוסף הופרדה לחיצה קצרה מגרירה כדי שאפשר יהיה גם ללטף וגם להזיז את הגור.
- תוקנה רגרסיית אינטראקציה ב-`PetHomeScreen`: הוגדל אזור הלחיצה וה-drop של כל גור, הלב עצמו הפך לכפתור פעיל, והאכלת גרירה חזרה לעבוד בלי לשבור את ההזזה של הגור.
- עודכנה לוגיקת התגמולים בבית הגורים: הלב מוצג תמיד אבל מתמלא רק כשהיצור באמת מבקש ליטוף, ואוכל נותן תגובת חיוך מיידית וכוכב רק אחרי מספר האכלות שמוגדר ב-`creatures.json` לכל יצור.
- מנגנון הצרכים בבית הגורים עודכן לקצב מהיר וברור יותר: בקשת ליטוף מופיעה כל `40` שניות, בועת הלב מופיעה רק כשהיא נדרשת וממלאת את עצמה עד הסוף בלחיצה אחת עם כוכב, ובקשת אוכל מופיעה בבועה מעל הראש עם מילוי אנימטיבי עד מצב שבע ותגובת חיוך בסיום.
- מנגנון הלב והאוכל הועבר למילוי מצטבר: הלב מתמלא בכמה לחיצות עד כוכב אחד, ובועת האוכל מתמלאת בכמה פריטי אוכל עד שהיצור נהיה שבע. ערכי הסף נשמרים ב-`creatures.json`.
- נוספו לחנות `כובע שמש` ו-`משקפי שמש`, ומנגנון ההלבשה של הגורים הורחב כך שאפשר להלביש עד `4` פריטים במקביל על כל גור.
